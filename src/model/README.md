User and Movie feature data are stored in cassandra db.

cassandra use keyspace 'model'.  It has 2 tables  'user'  'movie'

each of them, has column  'id'   'data'

data is the feature of user (movie)
